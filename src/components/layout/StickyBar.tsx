import { motion, AnimatePresence } from "framer-motion";
import { useState, useEffect } from "react";

export const StickyBar = () => {
  // This component is now empty - WhatsApp chat is in WhatsAppChat component
  return null;
};
